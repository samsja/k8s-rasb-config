# network troobleshooter

it is a simple docker image for network troobleshooting inside a k8s cluster ( or other) with the following install based on alpine:

* curl
* ping


```
kubectl run network-ts --image=samsja/network-ts                   
```
